# TaskTrek - Трекер Задач

![TaskTrek](https://img.shields.io/badge/version-1.0.0-blue)
![Platform](https://img.shields.io/badge/platform-Android-green)
![License](https://img.shields.io/badge/license-MIT-orange)

TaskTrek - это мощное веб-приложение для управления задачами, упакованное в Android APK.

## Особенности

- ✨ Современный интерфейс на React
- 📱 Адаптивный дизайн для мобильных устройств
- 💾 Экспорт данных в CSV и Excel
- 🚀 Красивая анимация загрузки
- 📊 Удобное управление задачами

## Быстрый старт

### Способ 1: Использование Docker (РЕКОМЕНДУЕТСЯ)

Самый простой способ - использовать Docker. Не требует установки Android SDK!

```bash
chmod +x build-apk-docker.sh
./build-apk-docker.sh
```

APK будет создан в папке `output/TaskTrek.apk`

### Способ 2: Локальная сборка

Требуется установленный Android SDK.

```bash
chmod +x build-apk.sh
./build-apk.sh
```

### Способ 3: Ручная сборка

```bash
cd cordova-app
cordova build android
```

## Требования

### Для Docker сборки:
- Docker Desktop

### Для локальной сборки:
- Node.js 14+
- Java JDK 11+
- Android SDK
- Apache Cordova

## Подробная документация

Полная инструкция по сборке APK находится в файле [README_APK_BUILD.md](README_APK_BUILD.md)

## Установка APK

После сборки APK файл можно установить на Android устройство:

1. **Через ADB:**
   ```bash
   adb install output/TaskTrek.apk
   ```

2. **Вручную:**
   - Перенесите APK на устройство
   - Включите "Установка из неизвестных источников"
   - Откройте файл и установите

## Структура проекта

```
TaskTrek/
├── index.html                  # Основное приложение
├── task_tracker.html           # Альтернативная версия
├── cordova-app/               # Проект Cordova
│   ├── www/                   # Веб-ресурсы
│   ├── config.xml            # Конфигурация приложения
│   └── platforms/            # Платформо-специфичный код
├── build-apk.sh              # Скрипт локальной сборки
├── build-apk-docker.sh       # Скрипт сборки через Docker
├── Dockerfile.build          # Docker образ для сборки
└── README_APK_BUILD.md       # Подробная инструкция
```

## Конфигурация приложения

Настройки приложения находятся в `cordova-app/config.xml`:

- **ID приложения:** com.tasktrek.app
- **Название:** TaskTrek
- **Версия:** 1.0.0
- **Минимальный Android SDK:** 24 (Android 7.0)
- **Целевой Android SDK:** 35

## Разработка

### Изменение HTML приложения

1. Отредактируйте `index.html` или `task_tracker.html`
2. Скопируйте измененные файлы в `cordova-app/www/`
3. Пересоберите APK

### Обновление конфигурации

Измените `cordova-app/config.xml` для настройки:
- Имени приложения
- ID пакета
- Разрешений
- Иконок и splash screens
- Других параметров

## Отладка

Для отладки приложения на устройстве:

```bash
cd cordova-app
cordova run android
```

Или используйте Chrome DevTools:
1. Подключите устройство
2. Откройте `chrome://inspect`
3. Выберите ваше приложение

## Создание Release версии

Для публикации в Google Play Store:

1. Создайте keystore:
   ```bash
   keytool -genkey -v -keystore tasktrek-release.keystore \
     -alias tasktrek -keyalg RSA -keysize 2048 -validity 10000
   ```

2. Создайте `build.json`:
   ```json
   {
     "android": {
       "release": {
         "keystore": "tasktrek-release.keystore",
         "storePassword": "ваш_пароль",
         "alias": "tasktrek",
         "password": "ваш_пароль"
       }
     }
   }
   ```

3. Соберите:
   ```bash
   cd cordova-app
   cordova build android --release
   ```

## Технологии

- **Apache Cordova** - платформа для создания нативных приложений
- **React** - библиотека для построения UI
- **Tailwind CSS** - CSS фреймворк
- **Papa Parse** - парсинг CSV
- **XLSX** - работа с Excel

## Поддержка

При возникновении проблем:
1. Проверьте [README_APK_BUILD.md](README_APK_BUILD.md)
2. Убедитесь, что все зависимости установлены
3. Попробуйте очистить проект: `cordova clean android`

## Лицензия

MIT License - свободно используйте для любых целей

## Автор

TaskTrek Team

---

**Версия:** 1.0.0
**Дата последнего обновления:** 2025-11-09
